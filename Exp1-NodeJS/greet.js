// greet.js - User-defined module for Experiment 1(e)
// Exports a function to greet a given course or name.

module.exports = function(name) {
  console.log(`Hello from the user-defined module! Welcome to ${name}.`);
};
